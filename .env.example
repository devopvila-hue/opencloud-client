# ============================================================
# OPENCloud Client Portal — Environment Variables
# ============================================================
# Copy to .env.local and fill in. Do NOT commit .env.local.
# ============================================================

# —  — Application —  — # The Client Portal is a pure Presentation Layer.
# It consumes the OPENCloud middleware API at /api/v1/*.
# In development, Vite proxies /api to http://127.0.0.1:3210.
# In production on Netlify, /api/* is served by the same host as
# the SPA (middleware and portal share a domain via reverse proxy).

# —  — Auth —  — # The session cookie (opc_session) is HttpOnly + HMAC-signed
# by the middleware. No client-side env needed for auth flows.

# —  — Auth redirect (authRedirect.ts) —  — # When the session cookie is missing, RequireAuth redirects
# the user to the login screen. By default the redirect uses
# window.location.origin (same-origin — Netlify SPA).
#
# Set VITE_AUTH_URL only when the login screen lives on a SEPARATE
# origin (e.g. a standalone apps/web application). Provide the full
# origin without a trailing slash — the SPA appends "/login".
#
# Examples:
#   Development (default — leave unset):
#     window.location.origin → http://localhost:5173/login
#
#   Same-origin production (Netlify — leave unset):
#     window.location.origin → https://portal.example.com/login
#
#   Separate-origin production (apps/web):
#     VITE_AUTH_URL=https://app.example.com
#     → https://app.example.com/login
#
# IMPORTANT: never hardcode URLs in source. Production bundles will
# ship them to real users.
# VITE_AUTH_URL=

# —  — Analytics (optional) —  — # Set to your analytics ID if you use a provider.
# VITE_ANALYTICS_ID=GA-XXXXXXXXX

# —  — Theme override (optional) —  — # Forces a theme on first load before localStorage kicks in.
# VITE_DEFAULT_THEME=dark

# —  — Netlify build settings (auto-detected) —  — # BUILD_COMMAND: npm run build
# PUBLISH_DIRECTORY: dist